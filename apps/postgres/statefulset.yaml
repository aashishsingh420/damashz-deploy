apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: damashz-postgres
spec:
  serviceName: damashz-postgres
  replicas: 1
  selector:
    matchLabels:
      app: damashz-postgres
  template:
    metadata:
      labels:
        app: damashz-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: damashz-postgres-secret
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 5Gi